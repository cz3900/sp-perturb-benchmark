# SPAC-seq subcutaneous primary-tumor cohort (subQ-1..5), Visium HD, cell-level.
# Built once on the server by scripts/spac_seq/build_cells.py (min_umi=2, dom=2.0; NT=control, no-guide=none).
adapter: spac_seq
adapter_kwargs:
  path: /home/yiru/database/spatial_perturbation_collection/SPAC-seq/processed/subQ.h5mu
k: 15
k_ref: 5
